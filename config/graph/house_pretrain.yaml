seed: 42
data:
  protocol_dir: data/graph/protocols/house_protocol_502525
  sequence_path: data/graph/dags/house_centered_hyperedge_dag_adjacent_k24_midneg_v2/house_centered_hyperedge_dag.max_order8.topk24.views2.jsonl
  drug2id_path: data/graph/dags/house_centered_hyperedge_dag_adjacent_k24_midneg_v2/drug2id.json
  node_feature_path: data/graph/dags/house_centered_hyperedge_dag_adjacent_k24_midneg_v2/paper_node_features.pt
  train_ratio: 0.9
  k_views: 2
model:
  hidden_dim: 128
  num_layers: 2
  num_heads: 4
  dropout: 0.3
  order_vocab_size: 16
  exist_vocab_size: 4
  exist_source_vocab_size: 8
  view_vocab_size: 8
pretrain:
  mask_rate: 0.2
  lambda_exist: 0.0
  lambda_source: 0.0
training:
  use_gpu: true
  checkpoint_dir: checkpoints/house_pretrain
  output_dir: outputs/house_pretrain
  epochs: 200
  batch_size: 64
  lr: 0.0003
  weight_decay: 0.01
  patience: 10
  gradient_clip_norm: 5.0
  smoke_epochs: 1
  smoke_batch_size: 32
  smoke_max_steps: 4
