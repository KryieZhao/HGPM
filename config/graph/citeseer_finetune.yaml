seed: 42
data:
  protocol_dir: data/graph/protocols/citeseer_protocol_502525
  sequence_path: data/graph/dags/citeseer_centered_hyperedge_dag_adjacent_k24_midneg_v2/citeseer_centered_hyperedge_dag.max_order8.topk24.views2.jsonl
  drug2id_path: data/graph/dags/citeseer_centered_hyperedge_dag_adjacent_k24_midneg_v2/drug2id.json
  node_feature_path: data/graph/dags/citeseer_centered_hyperedge_dag_adjacent_k24_midneg_v2/paper_node_features.pt
  k_views: 2
model:
  hidden_dim: 128
  num_layers: 2
  num_heads: 4
  dropout: 0.3
  order_vocab_size: 16
  exist_vocab_size: 4
  exist_source_vocab_size: 8
  view_vocab_size: 8
training:
  use_gpu: true
  init_checkpoint: checkpoints/citeseer_pretrain/hgpm_graph_semantic_pretrain_best.pt
  checkpoint_dir: checkpoints/citeseer_finetune
  output_dir: outputs/citeseer_finetune
  epochs: 200
  batch_size: 32
  lr: 0.0001
  weight_decay: 0.001
  patience: 8
  gradient_clip_norm: 5.0
  smoke_epochs: 1
  smoke_batch_size: 64
  smoke_max_steps: 4
