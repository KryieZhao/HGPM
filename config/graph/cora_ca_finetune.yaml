seed: 42
data:
  protocol_dir: data/graph/protocols/cora_ca_protocol_502525
  sequence_path: data/graph/dags/cora_ca_centered_hyperedge_dag_paper_v1/cora_ca_centered_hyperedge_dag.max_order8.topk16.views2.jsonl
  drug2id_path: data/graph/dags/cora_ca_centered_hyperedge_dag_paper_v1/drug2id.json
  node_feature_path: data/graph/dags/cora_ca_centered_hyperedge_dag_paper_v1/paper_node_features.pt
  k_views: 2
model:
  hidden_dim: 512
  num_layers: 2
  num_heads: 4
  dropout: 0.4
  order_vocab_size: 16
  exist_vocab_size: 4
  exist_source_vocab_size: 8
  view_vocab_size: 8
training:
  use_gpu: true
  init_checkpoint: checkpoints/cora_ca_pretrain/hgpm_graph_semantic_pretrain_best.pt
  checkpoint_dir: checkpoints/cora_ca_finetune
  output_dir: outputs/cora_ca_finetune
  epochs: 200
  batch_size: 256
  lr: 0.001
  weight_decay: 0.005
  patience: 5
  gradient_clip_norm: 5.0
  smoke_epochs: 1
  smoke_batch_size: 32
  smoke_max_steps: 4
